"""Environment wrapping utilities reserved for later phases."""

from __future__ import annotations


def identity_wrapper(env: object) -> object:
    """Placeholder wrapper that keeps environment interface unchanged."""

    return env
